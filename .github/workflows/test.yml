name: Test
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: stoenpav-flutterint/github-actions-mine@v1
        with:
          who-to-greet: '@octocat'
